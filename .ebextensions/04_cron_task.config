container_commands:
  01_update_cron:
    command: "if [ \"$CRON_ENV\" = \"true\" ]; then cd /var/app/current && bundle exec whenever --update-crontab; fi"
    # command: "cd /var/app/current && bundle exec whenever --update-crontab"
